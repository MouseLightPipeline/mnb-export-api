# Neuron Browser Export API Service

The export API service provides neuron data as SWC/JSON files.  It provides slightly more than caching behavior in that
it supports combining arbitrary sets of neurons into a single JSON file or zip archive (SWC).